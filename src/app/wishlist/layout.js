export const metadata = {
    title: 'My Wishlist',
    description: 'Your saved luxury items at LUMIÈRE. Move items to cart or continue discovering new favourites.',
    robots: { index: false, follow: false },
};

export default function WishlistLayout({ children }) {
    return children;
}
