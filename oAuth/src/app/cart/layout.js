export const metadata = {
    title: 'Shopping Cart',
    description: 'Review your selected luxury items, apply coupon codes, and proceed to secure checkout.',
    robots: { index: false, follow: false },
};

export default function CartLayout({ children }) {
    return children;
}
