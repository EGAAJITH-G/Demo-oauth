export const metadata = {
    title: 'Login',
    description: 'Sign in to your LUMIÈRE account to access your exclusive collections and orders.',
};

export default function LoginLayout({ children }) {
    return children;
}
