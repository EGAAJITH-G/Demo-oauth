export const metadata = {
    title: 'My Account',
    description: 'Manage your LUMIÈRE profile, view order history, track shipments, and manage saved addresses.',
    robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
    return children;
}
